{% extends 'base.html.twig' %}

{% block title %}Bebe{% endblock %}

{% block body %}
    <h1>Bebe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bebe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ bebe.nom }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ bebe.age }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ bebe.taille }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bebe_crud_index') }}">back to list</a>

    <a href="{{ path('app_bebe_crud_edit', {'id': bebe.id}) }}">edit</a>

    {{ include('bebe_crud/_delete_form.html.twig') }}
{% endblock %}
