<form method="post" action="{{ path('app_mixeur_delete', {'id': mixeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mixeur.id) }}">
    <button class="btn">Delete</button>
</form>
